
# Headless WordPress

This folder will hold WordPress core (downloaded by DDEV) under `public/`.

Useful local commands:
- `pnpm so wp:init` — configure DDEV & install WP locally
- `pnpm so wp:plugins` — install headless plugins (WPGraphQL, ACF, WPGraphQL ACF)
